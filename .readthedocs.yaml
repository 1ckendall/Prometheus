version: 1

build:
  os: ubuntu-22.04
  tools:
    python: "3.14"

sphinx:
  configuration: docs/conf.py

python:
  install:
    # Path to the file containing your doc dependencies
    - requirements: docs/requirements.txt